First create a ``.env`` at the root of the project with the following content and correct open weather API key:

````
REACT_APP_API_KEY=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
````

then 

``npm i && npm run start``